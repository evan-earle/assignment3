{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "start": "npm install && bun run --watch src/index.ts",
    "build": "cd ../../ && npm run build"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.2.0",
    "elysia": "latest",
    "mongodb": "^6.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
